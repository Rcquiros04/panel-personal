/**
 * Copia este archivo a config.js y pon tu clave.
 * config.js está en .gitignore y no se sube al repositorio.
 *
 * La clave gratuita se saca en https://www.weatherbit.io/account/create
 */
window.CONFIG = {
  WEATHERBIT_KEY: "",
};
